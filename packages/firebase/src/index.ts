// Re-export everything from firebase
export { storage, firestore, auth } from "./firebase";

// Re-export everything from firebase-storage
export { getBucket, pushToStorage } from "./firebase-storage";
